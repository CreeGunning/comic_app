<%= form_with(model: custom_character) do |form| %>
  <% if custom_character.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(custom_character.errors.count, "error") %> prohibited this custom_character from being saved:</h2>

      <ul>
        <% custom_character.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :powers, style: "display: block" %>
    <%= form.text_area :powers %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
